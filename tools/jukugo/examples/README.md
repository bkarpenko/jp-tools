# Разборы: состояние и статусы

Одиннадцать разборов. Это не иллюстрации к методу, а **регрессионный материал**:
на них проверяется, что правка метода не испортила качество. У инструмента без кода
это единственная форма проверки, какая бывает.

Ревизия проведена автором метода 02.09.2026, структурные утверждения сверены по файлам.

| файл | статус | почему |
|---|---|---|
| `遭難.md` | **ЭТАЛОН** | По действующему шаблону, все секции в штатном виде. **Образец формата** |
| `説得.md` | **ЭТАЛОН** | По шаблону; показывает переразложение и случай без слота в схеме |
| `年金.md` | **ЭТАЛОН** | По шаблону; показывает недоопределённость вклада знака. Второй образец: у него японская ветка шага 0, которой нет у 遭難 |
| `刺身.md` | РАБОЧИЙ | Содержание выправлено, формат близок; слота вольной развёртки нет |
| `推測.md` | РАБОЧИЙ | Верен, шесть секций; слота вольной развёртки нет |
| `日課.md` | устарел по форме | Дошаблонный; содержательно один из лучших — здесь родились шаг 3Б и запрет нормализации написания. Переделывать, приоритет низкий |
| `木刀.md` | устарел по форме | Дошаблонный; здесь родился четвёртый случай привязки — продуктивная модель. Переделывать, приоритет низкий |
| `心地.md` | устарел по форме | Дошаблонный и структурно самый нестандартный: шесть заголовков уровня `#`, нумерации секций нет, два параллельных блока «СЛОВО А / СЛОВО Б». Приоритет средний |
| `気配.md` | **устарел, вводит в заблуждение** | Содержит две редакции подряд в одном файле: два заголовка уровня `#`, 20 секций. Читающему не видно, какая действует. **Приоритет высокий** |
| `見た目.md` | устарел, дефект | Шаг 1 не выполнен по собственному признанию файла (статьи 目 нет), таблицы уверенности нет вовсе. Не выбрасывать, но и не переделывать: сначала запросить статью 目 |
| `語源.md` | **устарел по содержанию** | Значения выбраны по китайским параллелям при японском сложении — нарушение шага 0. **Переделывать первым:** тот же разбор служит рабочим примером правила в `../references/02_отбор_значений.md` §8, то есть ошибка заражает документацию |

## Порядок переделки, по цене ошибки

`語源` (ошибка метода, плюс заражает документацию) → `気配` (две редакции в одном файле)
→ `心地` → `日課`, `木刀` → `見た目` (только после того, как придёт статья 目).

## Расхождение формата — где оно на самом деле

Первоначальное подозрение было, что часть разборов не имеет секции запроса данных.
**Оно не подтвердилось:** секция есть во всех одиннадцати, но названа пятью способами —
«Что запросить», «Что запросить и зачем», «Что осталось», «Что осталось открытым»,
«Что закрыло бы открытые пункты». Расхождение в именовании, а не в наличии.

Настоящее расхождение другое: `../references/структура_вывода.md` §8 объявляет **вольную
развёртку** обязательной секцией с жёстким слотом, а из восьми непересобранных разборов
она есть ровно в одном — `気配`, том самом, ради которого правило и писалось. Ни один
разбор после него слот не заполнил. См. `../docs/открытое.md`.
